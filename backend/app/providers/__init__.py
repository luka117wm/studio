"""Внешние вызовы (принцип 5): контракт провайдера, маршрутизация, единая точка платного вызова.

Пайплайн не знает ID моделей и цен — только этап и запрос; модель выбирает `registry.py` по
`config/providers.yaml`, цену считает `cost/pricing.py`. Правила — `docs/providers.md`.
"""
