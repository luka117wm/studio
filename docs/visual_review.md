# Визуальная приёмка M1 (2026-09-19)

Попарная сверка приложения с артбордами `design/Studio - N *.dc.html` на 1536 (артборды существуют только в этой
ширине; 1920 и 2560 — по `layout.md`). Сверялись раскладка зон, ширины панелей (замер DOM в обоих), плотность,
кегли, отступы; цвета — по переменным `tokens.css` (hex артбордов → токены по `design/CLAUDE.md`).
Содержимое экранов (карточки, таблицы, формы) — не дефект: его ещё нет. Регрессия: `pnpm -C frontend e2e`,
44 эталона в `frontend/e2e/__screenshots__/` (11 экранов × 3 ширины + оверлеи и 8 вкладок каталога на 1536).

Решения: **исправлено** — в коде этой сессии (коммит M1.6); **осознанно** — отличается, причина указана;
**макет** — требует правки макета или handoff.

## Оболочка и раскладка

| Экран | Что не совпало | Решение |
|---|---|---|
| все | Переключатель канала: в макете кнопка 109px с квадратным аватаром и инициалом; у нас `Select` 176 с шевроном | осознанно — у `Select` нет слота иконки; свой `ChannelSwitcher` в M3 вместе с «Все каналы» |
| все | «?» в шапке: в макете текстовый глиф, у нас иконка `CircleQuestionMark` | осознанно — `IconButton` принимает только иконку; тот же смысл |
| все | Подсказки статус-строки: в макете плоский текст «⌘K команды», у нас чипы `KeyHint` из реестра | осознанно — M1.4: список только из реестра, чипы читаются лучше |
| 1 Выпуски | «⌘K команды», «⌘N новый выпуск» в статус-строке | макет — ⌘N браузерное, ⌘K без обработчика (задание M1.4 п. 4) |
| 1 Выпуски | Подпись «⌘N — новый выпуск» под пустым состоянием | исправлено — убрана, сочетания нет |
| 1 Выпуски | Фильтр «Все / Cursus / Otto», primary в заголовке, доска и слоты | содержимое — M3 |
| 2 Идеи | В макете левая панель «Семена ниши» 300 и правая «Идеи из кластеров» 340; у нас был только центр | исправлено — панели `seeds` 300 и `clusters` 340 в `ScreenLayout`; ширины из DOM артборда, в `layout.md` их нет |
| 3 Сценарий | Вкладки «Сценарий / План кадров / JSON» и счётчики в заголовке | содержимое — M4 |
| 3 Сценарий | Статус-строка макета: ⌘1…⌘3 переключают вкладки, а `layout.md` отдаёт ⌘1…⌘6 этапам | макет — конфликт внутри handoff, реализовано по `layout.md` |
| 4 Генерация | Полоса очередей 72 в макете на всю ширину над тремя колонками; у нас была внутри центра | исправлено — зона `strip` в `ScreenLayout` |
| 4 Генерация | Правая панель «Голос» 320 (у нас «Кадр») | содержимое — M6/M7 |
| 5 Монтаж | Ручка изменения высоты таймлайна 4px между центром и таймлайном; у нас центр был на 4px выше | исправлено — статичная ручка `data-zone="handle"`, перетаскивание — M8 |
| 6 Инспектор | Артборд 6 — лист из трёх вариантов инспектора по 320, не экран | осознанно — маршрут `/episodes/:id/edit/:shot` показывает монтаж с инспектором кадра |
| 7 Экспорт | Очередь рендера 216 внизу на всю ширину (шапка 28 + список); у нас её не было | исправлено — нижняя зона `queue` 216 |
| 7 Экспорт | Лог ffmpeg 420 внутри очереди справа | осознанно — по `layout.md` лог открывается кнопкой поверх правой части, на 2560 — третья колонка; реализация — M9 |
| 8 Публикация | Правая панель «Загрузка» 320; у нас её не было | исправлено |
| 9 Настройки | нав 224 + шапка 28 + содержимое | совпадает |
| 10 Состояния | 4 группы в SegmentedControl верхней панели, статус-строка меняется по группе | осознанно — 8 секций во вкладках заголовка (задание M1.5 добавило группы кита и клавиатуру); тексты статус-строки лежат в `productSections[].status` до контекстной статус-строки |
| 12 Канон | Левой панели в макете нет: слои — переключатель в заголовке; у нас была левая панель 280 | исправлено — SegmentedControl «Стиль / Периоды / Персонажи» в заголовке |
| 12 Канон | Правая панель «Опорные кадры стиля» 480 | макет — `layout.md` ограничивает правую колонку 280–420; оставлено 320, ждёт правки handoff |
| 12 Канон | Верхняя панель: «Канон · уровень канала, не выпуска» вместо названия выпуска | осознанно — TopBar показывает открытый выпуск; переделать в M5 |
| 1536 / 1920 / 2560 | Ширины зон замерены Playwright: библиотека 280→240 (<1440), инспектор 320→280, метаданные 380, пресеты 320, нав 224, лист 760, таймлайн 260 / 160 / 320, подписи дорожек 72, на 2560 колонка лога 420 | совпадает с `layout.md` |

## Кит

| Компонент | Что не совпало | Решение |
|---|---|---|
| `Dialog` | Оверлей клавиш 1000px с note в шапке (артборды 5, 10); было lg 640 | исправлено — `size="xl"` (1000), `note`; `KeyboardHelp` — три колонки |
| `Dialog` | Подвал оверлея «Сочетания меняются в настройках, раздел «Горячие клавиши»» | макет — раздела нет до M11; подвал «Esc или ? закрывает» |
| `EmptyState` | Иконка 24 в контейнере 44 с рамкой, описание 12/17 (артборд 10); было 16/32 и 13 | исправлено — исключение из правила «иконки 16» задокументировано в компоненте |
| `Toast` | Кнопка «Скрыть» (артборды 1, 5, 10); было только действие | исправлено — «Скрыть», когда действия нет |
| `Button` | «Обновить радар (38 запросов квоты, ~$0.12)» — пояснение и цена в одних скобках | исправлено — `priceNote` |
| `IconButton` | Кнопка-переключатель (пауза / продолжить) без состояния | исправлено — `pressed` → `aria-pressed` + заливка |
| `KeyHint` | Чипы клавиш в оверлее 20px, Plex Condensed 11/600 | исправлено — `size="md"` |
| `Tabs` | В заголовке экрана нижняя граница дублировала границу заголовка; снималось хаком `className` | исправлено — `border={false}` |
| `Button` primary sm | 24 против 22 в полосе «Черновой тайминг» | осознанно — шкала контролов 28/24 (`components.md`) |
| `Checkbox` | Радиус 2 против 3 в таблице расхождений | осознанно — `components.md`: клипы 2, контролы 4; 3 нет в токенах |
| `StatusGlyph` | В статус-строке 8–9px, у нас 16 масштабированный `size-3` | осознанно — форма читается; отдельный размер не нужен |
| `ProgressBar` | Шаги процесса: done белая, run градиент, todo row-border; у нас accent / штриховка / пустой трек | осознанно — цвета статусов из токенов; ступенчатый режим появится с ProcessCard в ките |
| `Select` | Нет слота иконки для аватара канала | осознанно — M3 (`ChannelSwitcher`) |
| `Slider` | disabled — ручка accent с opacity | осознанно — в макетах не описано |
| `NumberInput` | В фокусе внешняя смена `value` не видна (черновик ввода) | осознанно — задокументировано в компоненте |
| все | `className` не мёржит конфликтующие утилиты | осознанно — tailwind-merge не в стеке; порядок классов в компонентах учитывает это |
| — | ProcessCard / ErrorCard / ConflictBar / таблица расхождений есть в `components.md`, в ките нет | осознанно — собраны в `screens/states/demos/productCards.tsx`, переносятся в кит модулем состояний |
| — | `Skeleton`, `Divider`, `ScrollArea`, `Tooltip`, `Popover`, `DropdownMenu`, `Tabs`, `ProgressBar`, `KeyHint` в `components.md` отсутствуют | макет — добавить в handoff (решение пользователя M1.2) |

Незакрытых расхождений по раскладке и киту нет. Первичный список M1.5 — `docs/m1_review_list.md` (архив).
