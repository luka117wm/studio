# Roadmap

Линейный порядок модулей. Правило: **детально расписан только текущий модуль**; следующий составляется после
закрытия текущего, по его итогам в `STATUS.md`; остальные — одной строкой, чтобы помнить направление.
Модуль = устав `docs/tasks/Mk.md` + этапы `docs/tasks/Mk.x.md`. Материал для будущих модулей — `docs/drafts/`.
Статусы: ☑ сделано · ◐ в работе · ☐ не начат.

Финиш этапа: ☑ здесь + запись в `STATUS.md` + коммит `Mk.x: …`. Финиш модуля: приёмка из устава + `git tag mk`.

## ☑ M0 — Каркас проекта
`5a47c51`. Хвосты (tests/, ruff/mypy, лишний `src/studio/`) — закрыть в M2, см. `docs/drafts/B0.1.md`.

## ◐ M1 — Оболочка, токены, UI-кит, каталог состояний · `docs/tasks/M1.md`
- ☑ M1.1 Токены и типографика · `b0db367`
- ☑ M1.2 UI-кит: примитивы · `743bf12`
- ☑ M1.3 Оболочка и навигация · `44ebee8`
- ☑ M1.4 Клавиатура и фокус · `3a63737`
- ☑ M1.5 Каталог состояний (экран 10) · `b7d4cb2`
- ☐ M1.6 Визуальная приёмка → тег `m1`

## ☐ M2 — Бэкенд-фундамент (контур; этапы пишутся после M1)
Контракт `director.json` → хранилище и `project.json` → очередь джобов и SSE → провайдеры и учёт расходов →
typegen из Pydantic (заменяет временные типы M1.3). Хвосты M0. Черновики: `docs/drafts/B0.2`–`B0.6`.
Почему раньше экранов — см. `docs/tasks/M1.md`, «Что идёт следом».

## Дальше — направление, одной строкой (порядок экранов из `design/handoff/stack.md`)
- M3 — Выпуски и календарь слотов (экран 1)
- M4 — Идеи, сценарий и план: три режима, мост через чат, валидация JSON, смета (экраны 2–3)
- M5 — Канон: схемы, опорные портреты, сборщик промпта, экран 12, проверка консистентности (черновики `B2A.*`)
- M6 — Генерация: очередь с паузой, версии кадров, кэш по хэшу (экран 4)
- M7 — Голос, выравнивание и движение: `motion_spec.md`, единая математика TS/Python, golden-фикстуры
- M8 — Монтаж: таймлайн, retime по голосу, инспектор кадра (экран 5); анимация и SFX (экран 6)
- M9 — Рендер и экспорт: компоновщик, цвет BT.709, пресеты, FCP7/OTIO/SRT (экран 7)
- M10 — Публикация на YouTube (экран 8)
- M11 — Настройки: провайдеры, маршрутизация, бюджеты, расходы, каналы (экран 9)

Состав и порядок после M2 не зафиксированы: уточняются при составлении каждого модуля.
