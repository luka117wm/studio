# Черновики этапов

Написаны 11.09.2026, до импорта дизайна, под старую нумерацию «B-блоков». **Это материал, а не план.**
Когда очередной модуль составляется (после закрытия предыдущего, по `STATUS.md`), нужные черновики берутся отсюда,
переписываются под `docs/tasks/_TEMPLATE.md` и текущее состояние кода и переезжают в `docs/tasks/` уже с номером
модуля (`M2.1`, …). Ссылки внутри черновиков друг на друга и на `CLAUDE.md` могут быть устаревшими — сверять.

| Черновик | Куда, ориентировочно |
|---|---|
| `B0.1` — Каркас и запуск | выполнен как M0; хвосты — в файле, закрыть в M2 |
| `B0.2` — Контракт director.json | M2 |
| `B0.3` — project.json и хранилище | M2 |
| `B0.4` — Очередь джобов и SSE | M2 |
| `B0.5` — Провайдеры и учёт расходов | M2 |
| `B0.6` — Типы из Pydantic (typegen) | M2, последний этап |
| `B2A.1`–`B2A.5` — Канон: схемы, опорные портреты, сборщик промпта, экран, проверка | модуль канона, перед генерацией |
