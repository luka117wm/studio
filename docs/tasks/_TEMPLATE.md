# Mk.x — <название>

**Модуль:** Mk — <название модуля> · **Оценка:** N ч · **Модель:** Opus / Sonnet · **План до кода:** да / нет

## Цель
Одно предложение: что будет работать после этапа. Без «улучшить», «отрефакторить», «подготовить почву».

## Читать
- `путь/один.py`
- `docs/спека.md`
- для M-этапов — ещё `design/CLAUDE.md` и нужные разделы `design/handoff/components.md` / `layout.md`

Больше ничего не открывать. Если для работы нужен файл вне списка — сначала сказать об этом и дождаться ответа.

## Создать / изменить
- `путь/новый.py` — что делает
- `путь/существующий.ts` — какая часть меняется

## Не трогать
- `engine/motion.py` — меняется только через процедуру принципа 2
- всё, чего нет в списке выше

## Что сделать
1. …
2. …
(6–12 пунктов, каждый — законченное действие)

## Критерии приёмки
- [ ] `команда` завершается успешно
- [ ] тест `tests/test_x.py::test_y` зелёный
- [ ] вручную: <действие> → <наблюдаемый результат>

## Команды проверки
```bash
uv run pytest tests/test_x.py -v
```

## Чего не делать
- не добавлять зависимости без спроса
- не менять формат данных, не обновив `docs/`
- не «заодно» чинить соседнее

## После завершения
1. Тесты зелёные.
2. Менялся контракт → обновить `docs/…`.
3. Грабли → запись в `docs/LESSONS.md`.
4. 5–10 строк в `STATUS.md` (сделано, решения, хвосты, дальше) и ☑ в `docs/roadmap.md`.
5. `git commit -m "Mk.x: …"`. Тег — только на модуль (`git tag mk`), по приёмке из `docs/tasks/Mk.md`.
