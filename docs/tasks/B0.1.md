# B0.1 — Каркас и запуск

**Блок:** B0 — Фундамент · **Оценка:** 3 ч · **Модель:** Sonnet · **План до кода:** нет

> **Статус (2026-09-17):** выполнен как **M0** (`5a47c51`) частично. Есть: `pyproject.toml`, `backend/app/main.py`
> с `/api/health` и CORS, `frontend/` (Vite 8, React 19, TS strict, Tailwind v4, oxlint вместо eslint), прокси `/api`,
> `run.sh`, `.gitignore`, `backend/.env.example`. **Хвосты** (закрыть отдельным этапом или вместе с B0.2):
> `tests/test_health.py`, дымовой тест фронта, конфиг ruff (line-length 100) и mypy (strict для `app/`) в `pyproject`,
> убрать остаток `uv init` (`src/studio/`, `[project.scripts]`, `uv_build`). `vitest.config.ts` не нужен — vitest
> берёт `vite.config.ts`.

## Цель
Одной командой поднимаются бэкенд и фронтенд, линтеры и тесты проходят на пустом проекте.

## Читать
- `CLAUDE.md` (разделы «Стек», «Структура», «Соглашения»)

Репозиторий пустой, читать больше нечего.

## Создать / изменить
- `pyproject.toml` — uv, зависимости бэкенда, конфиг ruff и mypy
- `backend/app/main.py` — FastAPI, CORS для `http://localhost:5173`, `GET /api/health`
- `backend/app/api/__init__.py` — роутер-агрегатор
- `frontend/` — Vite + React 19 + TS strict + Tailwind, прокси `/api` → `:8000`
- `frontend/eslint.config.js`, `frontend/vitest.config.ts`
- `run.sh` — поднимает оба процесса, корректно гасит по Ctrl+C
- `.gitignore` — `data/`, `.env`, `node_modules`, `__pycache__`, `.venv`
- `tests/test_health.py`, `frontend/src/App.test.tsx` — по одному дымовому тесту
- `.env.example` — перечень ключей без значений

## Не трогать
- ничего: проект пустой

## Что сделать
1. `uv init`, зависимости из CLAUDE.md, но **только** те, что нужны на этом этапе: fastapi, uvicorn, pydantic, pytest. Остальные добавляются в своих блоках.
2. Настроить ruff (line-length 100) и mypy (strict для `app/`).
3. FastAPI с одним роутером и `/api/health`, отдающим `{"status": "ok", "version": "0.1.0"}`.
4. Vite-проект в `frontend/`, TS strict, Tailwind с дефолтным конфигом (токены придут в B0.6).
5. Прокси в `vite.config.ts`.
6. `run.sh`: параллельный запуск, `trap` на SIGINT, вывод обоих логов с префиксами.
7. Дымовые тесты.
8. Первый коммит.

## Критерии приёмки
- [ ] `./run.sh` поднимает оба сервиса, Ctrl+C гасит оба без осиротевших процессов
- [ ] `curl localhost:8000/api/health` → `{"status":"ok","version":"0.1.0"}`
- [ ] `http://localhost:5173` открывается, запрос к `/api/health` через прокси проходит
- [ ] `uv run ruff check . && uv run mypy backend` чисто
- [ ] `uv run pytest && pnpm -C frontend test` зелёные

## Команды проверки
```bash
./run.sh &
sleep 5 && curl -s localhost:8000/api/health
uv run ruff check . && uv run mypy backend
uv run pytest -q && pnpm -C frontend test --run
```

## Чего не делать
- не ставить Docker, авторизацию, роутер фронта, state-менеджер
- не создавать пустые папки «на будущее»
- не тащить зависимости следующих блоков
